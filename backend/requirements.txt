# WS5 core (mock path - no API keys needed)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0

# Tests
pytest>=8.0,<9.0
httpx>=0.27,<1.0            # required by fastapi.testclient

# Optional real providers (only needed when you switch off mock):
#   pip install openai       # DASFAX_LLM_PROVIDER=openai
#   pip install tavily-python # DASFAX_SEARCH_PROVIDER=tavily
